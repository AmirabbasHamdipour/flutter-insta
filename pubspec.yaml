name: rss_reader
description: یک برنامه خواننده RSS با قابلیت‌های پیشرفته
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Networking
  http:
  xml:

  # Local storage
  hive:
  hive_flutter:
  path_provider:

  # State management
  provider:

  # UI
  cached_network_image:
  flutter_html:
  video_player:
  chewie:
  flutter_localizations:
    sdk: flutter
  intl:

  # Settings
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true