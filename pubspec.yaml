name: todo_app
description: اپلیکیشن مدیریت کارها با قابلیت ذخیره‌سازی دائمی
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  path:
  intl:
  provider:
  flutter_slidable:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true